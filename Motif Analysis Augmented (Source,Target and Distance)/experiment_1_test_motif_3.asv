%{
    Script written by Yacine Mahdid at 2019-12-01 for the review of the
    motif analysis paper. 
    The goal is to get the source, target and the average distance of the
    motif connection.
%}

% we will have to filter the matrices to not h

%% Using fake data to test the motif_3.m script inside the NA library

% Variables initialization
network = rand(129,129);
[int, coh, freq, norm_int, norm_coh, norm_freq] = motif_3(network,number_rand_network, bin_swaps, weight_frequency);